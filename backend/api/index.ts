// Vercel Serverless Function Entry Point
// This file is handled by @vercel/node runtime and doesn't need to be in rootDir
import app from '../src/app.js';

// Export the Express app as the default handler for Vercel
export default app;
